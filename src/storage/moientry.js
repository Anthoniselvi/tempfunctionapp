const saveEntry = (entryToSave) => {
  // Get All Entries
  // Add this new entry to existing array
  // Save the new Array into local storage
};

const getAllEntriesForEvent = (eventId) => {
  // Get Total Entries
  // Filter by given eventId
  // Return the filtered array
};

const getAllEntries = () => {
  // Get Total Entries
  // Return total entries
};
